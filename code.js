/*const firstName = 'Aashith'
const age = 2
const final = `I'm ${firstName} and my age is ${age * 2}`
console.log(final)
const adult = age > 18
if (adult) {
    console.log(`${firstName} is an adult is ${adult}`);
} else {
    console.log(`${firstName} is an adult is ${adult}`);
}
console.log(String(age) + 11)
console.log(Number(final) - 1)
if (age === '2') {
    console.log(`Type conversion`);
}
*/
const hasDrivingLicense = true;
const hasGoodVision = false;
console.log(hasDrivingLicense || hasGoodVision);
